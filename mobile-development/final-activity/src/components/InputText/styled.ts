import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  inputContent: {
    marginBottom: 24,
    borderBottomWidth: 1,
    borderColor: '#ccc',
    fontSize: 20,
    lineHeight: 30,
    color: '#444',
  },
})